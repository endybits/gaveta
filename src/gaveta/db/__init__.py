"""Storage: the ORM model, its column types, and the Alembic migrations.

The migration scripts live *inside* this package (`db/migrations/`) rather than at the
repository root, so the wheel ships them and an installed Gaveta can create its own
database. See docs/adr/ADR-002-persistence-and-time.md.
"""
